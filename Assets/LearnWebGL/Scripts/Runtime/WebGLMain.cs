using UnityEngine;

namespace LearnWebGL.Scripts.Runtime
{
    public class WebGLMain : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("WebGLMain start!");
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
